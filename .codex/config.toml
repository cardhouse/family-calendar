# Important: use top-level key "mcp_servers' (not mcpServers)
[mcp_servers.herd]
command = "php"
args = [
    "/Applications/Herd.app/Contents/Resources/herd-mcp.phar"
]
env = { SITE_PATH = "." }

[mcp_servers.laravel-boost]
command = "php"
args = ["artisan", "boost:mcp"]
cwd = "/Users/cardhouse/Herd/family-calendar"
